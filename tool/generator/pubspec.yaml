name: generator
description: Internal tool that downloads IANA language registries and generates Dart sources.
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
